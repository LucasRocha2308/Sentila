import styled from "styled-components";

export const Container = styled.div`
  text-align: center;
  color: var(--white);
  margin: 6rem 0;
  p {
    margin: 1rem 0;
    min-height: 80px;
  }
  a {
    color: var(--light-blue);
    font-weight: 700;
  }
  margin-bottom: 7rem;
`;
