import styled from "styled-components";

export const Container = styled.div`
    text-align: center;
    color: var(--white);
    margin: 4rem 0 1rem;

    p {
        margin: 1rem 0;
    }
    a {
        color: var(--light-blue);
        font-weight: 700;
    }
    margin-bottom: 8rem;
`;
